import { styled } from "styled-components";

export const StyledContainer = styled.div`
display: flex;
flex-direction: column;
h2{
    margin: 0 auto;
    margin-bottom: 25px;
}

  form{
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
`;
